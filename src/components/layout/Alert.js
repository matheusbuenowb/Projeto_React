

function Alert({msg}){

    /*var msgfinal = ''

    const resultado = (msg === 'delete') ? msgfinal = 'Sucesso ao remover o projeto!':
                  (msg === 'success') ? msgfinal = 'Sucesso ao atualizar os dados do projeto!' :
                  "Erro";
    */
    return (
        alert(`Sucesso ao remover Projeto!`)
    )
}

export default Alert